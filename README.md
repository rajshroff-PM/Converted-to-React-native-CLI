# Converted React Native Project

Generated by React Native Converter AI.

## Setup
1. Copy these files into a new React Native CLI project.
2. Run `npm install`.
3. Run `npx react-native run-android` or `run-ios`.